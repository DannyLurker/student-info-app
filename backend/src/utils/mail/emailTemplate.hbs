<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OTP Verification</title>
    <style>
      body { font-family: "Inter", Arial, sans-serif; background-color: #f4f6f8;
      margin: 0; padding: 0; } .container { max-width: 520px; margin: 40px auto;
      background-color: #ffffff; border-radius: 16px; box-shadow: 0 4px 20px
      rgba(0, 0, 0, 0.08); overflow: hidden; } .header { background:
      linear-gradient(135deg, #4caf50, #43a047); color: #ffffff; text-align:
      center; padding: 28px 20px; } .header h1 { margin: 0; font-size: 26px;
      letter-spacing: 0.5px; } .content { padding: 28px; color: #333333; }
      .content p { margin: 12px 0; font-size: 15px; line-height: 1.6; } .otp-box
      { background-color: #f9f9f9; border: 1px dashed #4caf50; border-radius:
      10px; text-align: center; margin: 24px 0; padding: 16px; } .otp {
      font-size: 28px; font-weight: 700; color: #2e7d32; letter-spacing: 4px; }
      .warning { color: #e53935; font-weight: 600; text-align: center;
      margin-top: 20px; } .footer { background-color: #fafafa; text-align:
      center; padding: 18px; font-size: 14px; color: #777777; border-top: 1px
      solid #eeeeee; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>{{title}}</h1>
      </div>
      <div class="content">
        <p>Hello, <strong>{{username}}</strong></p>
        <p>{{message}}</p>
        <div class="otp-box">
          <div class="otp">{{otp}}</div>
        </div>
        <p class="warning">
          If you didn't request this, please ignore this email.
        </p>
      </div>
      <div class="footer">
        <p>Thank you for using our service.</p>
        <p>Sent at {{time}}</p>
      </div>
    </div>
  </body>
</html>